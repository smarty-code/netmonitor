# Changelog

## 1.1.0

- Settings: place the indicator on the left, center, or right of the top bar, plus order within that area.
- Production install path for GitHub: dependency checks, enable the extension, pack zip, CI, and tagged releases.
- Agent recreates `$XDG_RUNTIME_DIR/netmonitor.sock` if logout deletes the socket file.

## 1.0.0

- GNOME Shell 50 top-bar speeds, process list, SIGTERM / SIGKILL.
- Python agent over a user Unix socket, driven by NetHogs.
